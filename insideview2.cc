#include <iostream>
#include <set>
#include <vector>
#include <stack>
using namespace std;

vector < string > identifyTheLiars(vector < string > relations, string liar) {
    stack<string> st1, st2;
    vector < vector<string> > To;

    for (vector<string>::iterator it = relations.begin(); it != relations.end(); ++it) {


    st1.push(liar);

    while (!st1.empty()) {
	
int main() {

